class account {
    id; // Number
    userName; //string
    password; // string

    constructor(id, userName, password) {
        this.id = null;
        this.userName = userName;
        this.password = password;
    }
}